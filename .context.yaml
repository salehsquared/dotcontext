version: 1
last_updated: 2026-02-13T18:00:21.312Z
fingerprint: a46a29b7
scope: .
summary: Folder-level documentation for LLMs — .context.yaml files for every directory
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
subdirectories:
  - name: docs/
    summary: Documentation.
  - name: scripts/
    summary: >
      The 'scripts' directory contains utility scripts for managing project workflows, including building grammar files,
      generating schemas, and configuring git hooks. These scripts automate essential tasks to ensure smooth development
      and maintenance of the project.
  - name: src/
    summary: >
      The `src` directory is responsible for implementing the core functionalities and command-line interface for
      managing context generation and diagnostics in a project. It provides various commands for initializing
      configurations, validating context files, and monitoring their freshness.
  - name: tests/
    summary: >
      The tests directory contains unit tests for various components of the project, ensuring functionality and
      correctness of features including command handling, generator processes, and utility functions. It uses the Vitest
      framework for structured testing across different modules.
project:
  name: dotcontext
  description: Folder-level documentation for LLMs — .context.yaml files for every directory
  language: typescript
  package_manager: npm
structure:
  - path: docs
    summary: Documentation.
  - path: scripts
    summary: >
      The 'scripts' directory contains utility scripts for managing project workflows, including building grammar files,
      generating schemas, and configuring git hooks. These scripts automate essential tasks to ensure smooth development
      and maintenance of the project.
  - path: src
    summary: >
      The `src` directory is responsible for implementing the core functionalities and command-line interface for
      managing context generation and diagnostics in a project. It provides various commands for initializing
      configurations, validating context files, and monitoring their freshness.
  - path: tests
    summary: >
      The tests directory contains unit tests for various components of the project, ensuring functionality and
      correctness of features including command handling, generator processes, and utility functions. It uses the Vitest
      framework for structured testing across different modules.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
  - project
  - structure
