version: 1
last_updated: 2026-02-12T18:06:56.017Z
fingerprint: fb2b555e
scope: src/utils
summary: >
  The src/utils directory contains utility functions for handling configuration, environment variables, display
  messages, and concurrency. It provides essential helpers to manage application settings and present user messages
  effectively.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
decisions:
  - what: Using async functions for file operations.
    why: To ensure non-blocking I/O operations for better performance.
    tradeoff: Increased complexity in error handling due to async nature.
constraints:
  - "Environment variable keys must follow the naming convention: /^[A-Za-z_][A-Za-z0-9_]*$/."
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
