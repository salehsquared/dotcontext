version: 1
last_updated: 2026-02-12T22:23:51.485Z
fingerprint: 0d727c99
scope: src/core
summary: >
  This directory contains core functionality for computing directory fingerprints, scanning project structures, and
  managing .context.yaml files. It ensures organized and efficient tracking of project files and their states.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
decisions:
  - what: The fingerprinting process is non-recursive and only considers files directly in the directory.
    why: This design choice simplifies fingerprint calculations and prevents excessive computation on nested directories.
    tradeoff: Potentially misses changes in files deeply nested within subdirectories.
constraints:
  - Do not include .context.yaml itself or common non-source directories when computing fingerprints.
  - Files and directories that match certain ignore patterns will be excluded from scans.
exports:
  - "async function computeFingerprint(dirPath: string, ignorePatterns: string[] = []): Promise<string>"
  - type FreshnessState
  - "async function checkFreshness( dirPath: string, storedFingerprint: string | undefined, ignorePatterns: string[] =
    [], ): Promise<"
  - AGENTS_FILENAME
  - "async function readAgentsMd(rootPath: string): Promise<string | null>"
  - "async function writeAgentsMd(rootPath: string, content: string): Promise<void>"
  - 'async function updateAgentsMd( rootPath: string, entries: AgentsEntry[], projectName: string, ): Promise<"created"
    | "appended" | "replaced" | "skipped">'
  - interface ScanResult
  - interface ScanOptions
  - "async function scanProject( rootPath: string, options: ScanOptions ="
  - "function groupByDepth(root: ScanResult): ScanResult[][]"
  - "function flattenBottomUp(result: ScanResult): ScanResult[]"
  - contextSchema
  - configSchema
  - type ContextFile
  - type ConfigFile
  - type FileEntry
  - type InterfaceEntry
  - type DecisionEntry
  - type SubdirectoryEntry
  - type ProjectMeta
  - type StructureEntry
  - type Evidence
  - CONTEXT_FILENAME
  - CONFIG_FILENAME
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - exports
