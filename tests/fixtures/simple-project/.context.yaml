version: 1
last_updated: 2026-02-12T04:45:34.938Z
fingerprint: 4135b1a1
scope: tests/fixtures/simple-project
summary: >
  This directory contains configuration files for a simple project using TypeScript, which compiles down to ES2022 and
  utilizes Node16 modules.
files:
  - name: package.json
    purpose: Defines the project metadata and type as a module.
  - name: tsconfig.json
    purpose: Configures TypeScript compilation options including target and module settings.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the files list, interfaces, and current_state sections.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
interfaces:
  - name: Main Function
    description: Functions as the entry point for the TypeScript application.
  - name: Addition Utility Function
    description: Performs addition of two numbers.
decisions:
  - what: Using ES2022 as the target.
    why: To leverage the latest JavaScript features.
    tradeoff: May not be compatible with older environments.
constraints:
  - Compilation is strictly configured to enforce type checks.
current_state:
  working:
    - TypeScript compiles without errors.
  in_progress:
    - Development of additional features and unit tests.
subdirectories:
  - name: src/
    summary: >
      This directory contains TypeScript source files for a simple project that includes a main function and a utility
      function for addition.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
