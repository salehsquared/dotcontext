version: 1
last_updated: 2026-02-12T22:23:42.723Z
fingerprint: 46b5fc2a
scope: tests/fixtures/monorepo
summary: >
  This directory contains fixtures for testing within a monorepo structure, facilitating organized testing across
  multiple services by including specific packages like the API server and shared TypeScript types.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
decisions:
  - what: Utilizing Yarn workspaces for managing dependencies
    why: To simplify dependency management across multiple packages and enable a streamlined development workflow.
    tradeoff: Increased complexity in understanding workspace behavior for new developers.
constraints:
  - All packages must follow the naming convention to be recognized by the monorepo structure.
subdirectories:
  - name: packages/
    summary: >
      This directory contains fixtures used for testing within a monorepo structure. It includes specific packages like
      the API server and shared TypeScript types, facilitating organized testing across the services.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
