version: 1
last_updated: 2026-02-12T04:45:32.207Z
fingerprint: 46b5fc2a
scope: tests/fixtures/monorepo
summary: >
  The `tests/fixtures/monorepo` directory defines a monorepo structure with workspace support for managing multiple
  packages within a single repository.
files:
  - name: package.json
    purpose: Defines the monorepo configuration and workspace setup.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the files list, interfaces, and current_state sections.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
constraints:
  - Packages must be located under the 'packages/*' path to be recognized by the workspace.
subdirectories:
  - name: packages/
    summary: >
      The `tests/fixtures/monorepo/packages` directory contains two primary packages for a monorepo setup, providing
      essential functionality and shared components for API handling and shared configurations.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
