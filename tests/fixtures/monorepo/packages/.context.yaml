version: 1
last_updated: 2026-02-12T01:29:52.012Z
fingerprint: e3b0c442
scope: tests/fixtures/monorepo/packages
summary: >
  This directory contains subdirectories for handling different aspects of a monorepo, including an API package and a
  shared package with TypeScript type definitions. Each subdirectory is structured to facilitate modular development and
  reusability within the monorepo.
files:
  - name: api/
    purpose: Contains source code and configuration for the API package.
  - name: shared/
    purpose: Includes TypeScript type definitions for use across the monorepo.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the files list, interfaces, and current_state sections.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
constraints:
  - TypeScript type definitions in shared/ must be maintained for compatibility across different packages.
subdirectories:
  - name: api/
    summary: >
      This directory contains the source code and configuration for an API package within a monorepo, which is
      structured to handle API-related functionalities.
  - name: shared/
    summary: >
      This directory contains a shared package for a monorepo, which includes TypeScript type definitions for use across
      different parts of the monorepo.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
