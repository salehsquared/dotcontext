version: 1
last_updated: 2026-02-12T18:07:01.488Z
fingerprint: e3b0c442
scope: tests/fixtures
summary: >
  The tests/fixtures directory provides sample configurations and mock data for testing various project setups,
  including monorepos, simple projects, and context-specific scenarios. It facilitates structured and reusable test data
  management to support unit testing and integration testing processes.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
decisions:
  - what: Using a monorepo structure for fixtures
    why: To ensure cohesive interactions and shared definitions across multiple packages.
    tradeoff: Increased complexity in managing dependencies and workspace configurations.
constraints:
  - Test fixtures must be maintainable and relevant to the application's current context.
  - Mock data should be appropriately categorized to prevent overlap between different testing scenarios.
subdirectories:
  - name: monorepo/
    summary: >
      This directory serves as a fixture configuration for a monorepo setup utilizing workspaces to manage multiple
      packages. It ensures cohesive interactions and shared definitions across the various components of the project.
  - name: simple-project/
    summary: >
      This directory contains a simple TypeScript project setup for foundational testing purposes. It includes
      configuration files for building and managing the project structure, focusing on a minimal codebase that
      highlights essential TypeScript functionality.
  - name: with-contextignore/
    summary: >
      This directory contains test fixtures for unit testing purposes, including both permanent and temporary mock data
      and scenarios relevant to the application's context. It provides a structured way to manage and utilize test data
      during the testing process.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
