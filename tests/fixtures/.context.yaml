version: 1
last_updated: 2026-02-12T04:45:53.167Z
fingerprint: e3b0c442
scope: tests/fixtures
summary: >
  The `tests/fixtures` directory provides sample files and configurations for various testing scenarios, enabling the
  development and testing of code across different project structures.
files:
  - name: monorepo
    purpose: Defines a monorepo structure with workspace support for managing multiple packages.
  - name: simple-project
    purpose: Contains configuration files for a simple TypeScript project compiling to ES2022.
  - name: with-contextignore
    purpose: Includes configuration and test fixtures for a project focusing on context management.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the files list, interfaces, and current_state sections.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
constraints:
  - Projects must adhere to their respective configuration standards for TypeScript and context management.
subdirectories:
  - name: monorepo/
    summary: >
      The `tests/fixtures/monorepo` directory defines a monorepo structure with workspace support for managing multiple
      packages within a single repository.
  - name: simple-project/
    summary: >
      This directory contains configuration files for a simple project using TypeScript, which compiles down to ES2022
      and utilizes Node16 modules.
  - name: with-contextignore/
    summary: >
      This directory contains files relevant to the "with-contextignore" project, including configuration for the
      project and test fixtures for a testing framework. It serves as a workspace for developing and testing code with a
      specific focus on managing context-related files.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
