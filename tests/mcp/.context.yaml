version: 1
last_updated: 2026-02-12T22:23:51.344Z
fingerprint: 570d3896
scope: tests/mcp
summary: >
  This directory contains tests for the MCP (Model Context Protocol) components, including server initialization and
  tool functionality. It ensures that the behavior of the MCP server and associated tools meet the expected outcomes
  defined in the specifications.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
decisions:
  - what: Use of Vitest for testing framework
    why: Vitest provides a fast and efficient testing experience for TypeScript environments.
    tradeoff: While it has a learning curve, it offers improved performance compared to some established testing libraries.
constraints:
  - Tests must clean up any temporary files or directories created during execution.
  - Path traversal attempts in context queries will result in an error and should be rejected.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
