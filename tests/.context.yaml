version: 1
last_updated: 2026-02-12T18:07:26.224Z
fingerprint: ce633ea2
scope: tests
summary: >
  This directory contains unit tests for the project's functionalities, ensuring that various components work correctly
  through automated testing. It includes tests for detecting exports and dependencies, environmental configurations, and
  other core functionalities.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
decisions:
  - what: Use Vitest for testing framework
    why: Vitest provides a fast and efficient testing experience with modern features.
    tradeoff: Could limit compatibility with other libraries or environments not optimized for Vitest.
constraints:
  - Tests must not include secrets, API keys, or any sensitive information.
  - Expectations in tests should handle potential nulls or errors gracefully.
subdirectories:
  - name: commands/
    summary: >
      The tests/commands directory contains unit tests for various command functionalities including configuration
      management, initialization, and validation within the context of a software project. It ensures the integrity and
      correctness of command behaviors through automated testing using Vitest.
  - name: core/
    summary: >
      The tests/core directory contains tests for the core functionality of the markdown writer module. It verifies the
      reading, writing, and updating of AGENTS.md files in various scenarios to ensure data integrity and expected
      behavior.
  - name: fixtures/
    summary: >
      The tests/fixtures directory provides sample configurations and mock data for testing various project setups,
      including monorepos, simple projects, and context-specific scenarios. It facilitates structured and reusable test
      data management to support unit testing and integration testing processes.
  - name: generator/
    summary: >
      The tests/generator directory contains unit tests for the markdown generation functionality related to agents. It
      ensures that various markdown generation utilities work correctly by validating their output against expected
      results.
  - name: mcp/
    summary: >
      This directory contains unit tests for the MCP (Model Context Protocol) server and its associated tools. It
      validates functionality such as server startup, handling of context queries, freshness checks, and tool
      registration.
  - name: utils/
    summary: >
      This directory contains unit tests for utility functions, specifically for testing asynchronous operations with
      concurrency control. It ensures the correctness and reliability of the utility methods used within the
      application.
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - subdirectories
